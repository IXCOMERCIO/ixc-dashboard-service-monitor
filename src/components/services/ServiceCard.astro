---
import ArrowRight from "../../assets/ArrowRight.astro"
import DetailButton from "../hooks/DetailButton.astro";
export interface Props {
  title: string;
  description: string;
  status: 'Healthy' | 'Warning' | 'Critical';
  services: string;
  responseTime: string;
  detailsHref: string;
  historyHref: string;
}

const { title, description, status, services, responseTime, detailsHref, historyHref } = Astro.props;

const statusColor = {
  Healthy: 'bg-green-500',
  Warning: 'bg-yellow-500',
  Critical: 'bg-red-500',
};
---

<div class="rounded-lg border bg-white text-gray-800 shadow-sm overflow-hidden">
  <!-- Header -->
  <div class="flex flex-col space-y-1.5 p-6 pb-2">
    <div class="flex justify-between items-start">
      <h3 class="font-semibold tracking-tight text-lg">{title}</h3>
      <div class={`inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold text-white ${statusColor[status]}`}>
        {status}
      </div>
    </div>
  </div>

  <!-- Body -->
  <div class="p-6 pt-0 pb-2">
    <div class="text-sm text-gray-500 mb-4">{description}</div>

    <div class="grid grid-cols-2 gap-4 mb-4">
      <div>
        <div class="text-sm text-gray-500 mb-1">Services</div>
        <div class="flex items-center gap-2">
          <span class={`inline-flex w-3 h-3 rounded-full ${statusColor[status]}`}></span>
          <span class="font-medium">{services}</span>
        </div>
      </div>
      <div>
        <div class="text-sm text-gray-500 mb-1">Avg. Response</div>
        <div class="font-medium">{responseTime}</div>
      </div>
    </div>

    <div class="space-y-2">
      <div class="text-sm text-gray-500 mb-1">Service Health</div>
      <div class="w-full bg-gray-200 rounded-full h-2.5">
        <div class={`h-2.5 rounded-full ${statusColor[status]}`} style="width: 80%;"></div>
      </div>
    </div>
  </div>

  <!-- Footer buttons -->
  <div class="flex items-center p-6 pt-0 gap-4">
    <DetailButton href={detailsHref} title="Ver Detalles" />
    <DetailButton href={historyHref} title="Ver Histórico" />
  </div>
</div>
