---
import ArrowLeft from '../../assets/ArrowLeft.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="mt-4">
  <a href="/" class="inline-flex items-center text-gray-800 hover:text-gray-600 text-sm">
    <ArrowLeft /> {title}
  </a>
</div>